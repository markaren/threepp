// Moved to examples/libs/capture_util.hpp (on every example's include path),
// so GL-capable demos can use it too. This shim keeps the gitignored scratch
// harnesses' `#include "../capture_util.hpp"` working.
#pragma once
#include "../libs/capture_util.hpp"
